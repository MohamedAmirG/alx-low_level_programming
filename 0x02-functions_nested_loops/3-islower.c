#include "main.h"

/**
 * _islower - check for lowercase character.
 *
 * @c: the character to check.
 *
 * Return 1 if lower case and returns 0 if not.
 */
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
