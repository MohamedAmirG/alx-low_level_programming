#include "main.h"
#include <stdio.h>

/**
 * int _abs - computes the absolute value of an integer
 *
 * Return: the absolute value of an int.
 */
int int _abs(int)
{
if (n >= 0)
{
return (n);
}
return (-n);
}
